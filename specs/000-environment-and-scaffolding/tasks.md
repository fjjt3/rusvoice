# 000 · Environment and Scaffolding — Tasks

- [ ] Create project folder structure (`app/`, `tests/`).
- [ ] Create `.gitignore` to exclude `.venv/`, `__pycache__/`, and `.pytest_cache/`.
- [ ] Create `requirements.txt` with core dependencies (`fastapi`, `uvicorn`, `edge-tts`, `pydantic`, `pytest`, `pytest-asyncio`, `httpx`).
- [ ] Create `app/main.py` with a basic FastAPI app and `GET /health` endpoint.
- [ ] Create `tests/test_health.py` to verify the health endpoint status code (200).
- [ ] Validate implementation against acceptance criteria in `spec.md`.
- [ ] Move feature to "Done" in `../../constitution/roadmap.md`.