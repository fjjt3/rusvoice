# 000 · Environment and Scaffolding — Tasks

- [x] Create project folder structure (`app/`, `tests/`).
- [x] Create `.gitignore` to exclude `.venv/`, `__pycache__/`, and `.pytest_cache/`.
- [x] Create `requirements.txt` with core dependencies (`fastapi`, `uvicorn`, `edge-tts`, `pydantic`, `pytest`, `pytest-asyncio`, `httpx`).
- [x] Create `app/main.py` with a basic FastAPI app and `GET /health` endpoint.
- [x] Create `tests/test_health.py` to verify the health endpoint status code (200).
- [x] Validate implementation against acceptance criteria in `spec.md`.
- [x] Move feature to "Done" in `../../constitution/roadmap.md`.