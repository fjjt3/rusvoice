# 005 · Text Chunking Processor — Tasks

- [x] Create `app/utils/text_chunker.py`.
- [x] Implement `split_text_into_chunks(text: str, max_chunk_size: int = 1000) -> list[str]`.
- [x] Implement fallback splitting for long sentences without punctuation.
- [x] Create `tests/test_text_chunker.py` covering multi-sentence splitting, exact max-length bounds, and single short input handling.
- [x] Validate implementation against acceptance criteria in `spec.md`.
- [x] Update feature status in `spec.md` to `implemented ✅`.
- [x] Move feature to "Done" in `../../constitution/roadmap.md`.